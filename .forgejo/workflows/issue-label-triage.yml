# Human-activity triage: when a person (not the shared `xpufx` agent identity)
# opens an issue or posts an issue comment, make sure the exclusive
# `attention/0-orchestrator` label is the effective attention signal.
#
# Why a host-mode runner: this is a handful of curl/jq calls, so there is no
# reason to pay for a container image to be pulled/started. A runner registered
# with a `host`-backend label executes the steps directly on the host, which is
# faster and cheaper for small operations.
#
# RUNNER LABEL: this instance's host runner is registered as
# `runner-local-shell` (host backend, no container). `runs-on` matches the
# label name. No `container:` block is present on purpose: that is what selects
# the host backend.
#
# RUNTIME DEPENDENCIES: host mode runs on the host, so the host itself must
# provide `curl` and `jq`. The workflow fails loudly if either is missing.
#
# Trigger conditions
#   - issues        : opened   (a new issue)
#   - issue_comment : created  (a comment on an issue or PR)
#
# Actor / human-vs-`xpufx` check
#   All agents share the `@xpufx` forge account, so `github.actor == xpufx`
#   means the action was automated; anything else is treated as a human. The
#   job no-ops (exits 0) when the actor is `xpufx`.
#
# Exclusive-label logic
#   `attention/*` labels are `exclusive: true` in
#   `plugins/forges/examples/labels/label-base.yaml`, so the Forgejo DB evicts
#   any other `attention/*` label the moment a new one is added. This workflow
#   therefore only ADDS `attention/0-orchestrator` (via
#   `POST /repos/{owner}/{repo}/issues/{n}/labels`) and lets the scope
#   exclusivity evict `attention/1-agent`, `attention/2-user`, or
#   `attention/3-ignore`. It does not touch any other scope.
#
# Idempotence / loops
#   No-ops when `attention/0-orchestrator` is already present. The workflow only
#   writes a label (never a comment or a new issue), and adding a label emits an
#   `issues:labeled` event, which is not a trigger here, so it cannot re-trigger
#   itself.
name: Issue label triage (host runner)

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

jobs:
  triage:
    name: Ensure attention/0-orchestrator for human activity
    runs-on: runner-local-shell # host runner label; no container
    steps:
      - name: Set attention/0-orchestrator (human only)
        env:
          # Forgejo auto-injects the run's own task token under these names; it
          # carries write access to this repository.
          FORGEJO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          API_URL: ${{ github.api_url }}
          REPO: ${{ github.repository }}
          ACTOR: ${{ github.actor }}
          ISSUE: ${{ github.event.issue.number }}
        run: |
          set -eu

          command -v curl >/dev/null 2>&1 || { echo "::error::curl is required on the host runner"; exit 1; }
          command -v jq   >/dev/null 2>&1 || { echo "::error::jq is required on the host runner";   exit 1; }

          # All agents share the `xpufx` account: that actor is automated, not a
          # human, so there is nothing to do.
          if [ "$ACTOR" = "xpufx" ]; then
            echo "actor '$ACTOR' is the shared agent identity; skipping"
            exit 0
          fi

          auth_header="Authorization: token ${FORGEJO_TOKEN}"

          # Skip pull requests. The workflow targets issues; a PR comment shares
          # the same event shape, and the issues API reports `pull_request` for
          # a PR, so this is the authoritative check.
          issue_json="$(curl -fsS -H "$auth_header" "${API_URL}/repos/${REPO}/issues/${ISSUE}")"
          if [ "$(printf '%s' "$issue_json" | jq -r '.pull_request != null')" = "true" ]; then
            echo "issue #${ISSUE} is a pull request; skipping"
            exit 0
          fi

          # Already triaged: `attention/0-orchestrator` is present, no-op.
          labels_json="$(curl -fsS -H "$auth_header" "${API_URL}/repos/${REPO}/issues/${ISSUE}/labels")"
          if [ "$(printf '%s' "$labels_json" | jq -r 'any(.[]; .name == "attention/0-orchestrator")')" = "true" ]; then
            echo "issue #${ISSUE} already has attention/0-orchestrator; nothing to do"
            exit 0
          fi

          # Add it. Because `attention/*` is an exclusive scope, this single add
          # evicts whatever attention label is currently set.
          echo "human actor '${ACTOR}' on issue #${ISSUE}: setting attention/0-orchestrator"
          curl -fsS -X POST -H "$auth_header" -H "Content-Type: application/json" \
            -d '{"labels":["attention/0-orchestrator"]}' \
            "${API_URL}/repos/${REPO}/issues/${ISSUE}/labels" >/dev/null

          # Confirm the write landed (and that the label is defined on the repo);
          # a silent drop would otherwise leave the board's signal wrong.
          labels_json="$(curl -fsS -H "$auth_header" "${API_URL}/repos/${REPO}/issues/${ISSUE}/labels")"
          if [ "$(printf '%s' "$labels_json" | jq -r 'any(.[]; .name == "attention/0-orchestrator")')" != "true" ]; then
            echo "::error::attention/0-orchestrator was not applied to issue #${ISSUE} (is the label defined on the repo?)"
            exit 1
          fi
          echo "issue #${ISSUE}: attention/0-orchestrator set"
