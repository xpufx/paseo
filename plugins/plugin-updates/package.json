{
  "name": "plugin-updates",
  "private": true,
  "license": "MIT",
  "version": "0.1.0",
  "description": "Monitor installed Paseo plugins for git-source updates, with per-subdirectory status, source links, and one-click pulls.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npx tsx --test shared/updates.test.ts server/updates.test.ts client/orphans.test.ts",
    "test:vitest": "npm run typecheck && vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "@getpaseo/plugin": "0.8.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.10",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  }
}
